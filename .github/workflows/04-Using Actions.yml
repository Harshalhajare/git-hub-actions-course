name: 04- Using Actions

on: push

jobs:
  build:
    runs-on: Ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Pronting Folders
        run: ls
      
    

